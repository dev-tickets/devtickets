query Event($id: UUID){
  events: eventsCollection(filter: {id: {eq: $id}}) {
    edges {
      node {
        id
        name
        description
        city
        country
        start_date
        end_date
        image_link
        address
        map_link
      }
    }
  }
}
